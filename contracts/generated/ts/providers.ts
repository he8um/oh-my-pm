// This file is generated by tools/gen-contracts.mjs.
// Do not edit by hand.

import type { JsonValue, KernelWarning } from "./core.js";

/** Known context provider identifier. */
export const PROVIDER_ID_VALUES = ["local", "github", "clickup", "airtable", "linear", "jira", "notion"] as const;
export type ProviderId = (typeof PROVIDER_ID_VALUES)[number];

/** Read-only provider action. */
export const PROVIDER_ACTION_VALUES = ["search", "fetch", "list"] as const;
export type ProviderAction = (typeof PROVIDER_ACTION_VALUES)[number];

/** Read-only request to a context provider. */
export interface ProviderRequest {
  /** Target provider. */
  providerId: ProviderId;
  /** Requested action. */
  action: ProviderAction;
  /** Query text or identifier. */
  query: string;
  /** Maximum item count. */
  limit?: number;
}

/** Normalized item type across providers. */
export const NORMALIZED_ITEM_TYPE_VALUES = ["task", "document", "issue", "pullRequest", "record", "note", "unknown"] as const;
export type NormalizedItemType = (typeof NORMALIZED_ITEM_TYPE_VALUES)[number];

/** Provider item normalized to the shared shape. */
export interface NormalizedProviderItem {
  /** Provider-scoped item identifier. */
  id: string;
  /** Normalized item type. */
  type: NormalizedItemType;
  /** Item title. */
  title: string;
  /** Canonical item URL. */
  url?: string;
  /** Originating provider. */
  source: ProviderId;
  /** Normalized item payload. */
  data: JsonValue;
}

/** Normalized response from a context provider. */
export interface NormalizedProviderResponse {
  /** Responding provider. */
  providerId: ProviderId;
  /** Normalized items. */
  items: NormalizedProviderItem[];
  /** Warnings. */
  warnings?: KernelWarning[];
}
