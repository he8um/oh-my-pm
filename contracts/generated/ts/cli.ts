// This file is generated by tools/gen-contracts.mjs.
// Do not edit by hand.

import type { JsonValue, KernelWarning, Locale } from "./core.js";

/** CLI output mode. */
export const CLI_OUTPUT_MODE_VALUES = ["brief", "json", "markdown"] as const;
export type CliOutputMode = (typeof CLI_OUTPUT_MODE_VALUES)[number];

/** Canonical machine-readable CLI output envelope. */
export interface CliJsonOutput {
  /** Whether the command succeeded. */
  ok: boolean;
  /** Executed command name. */
  command: string;
  /** Command payload. */
  data: JsonValue;
  /** Warnings. */
  warnings?: KernelWarning[];
}

/** Status of a single doctor check. */
export const DOCTOR_CHECK_STATUS_VALUES = ["ok", "warn", "fail"] as const;
export type DoctorCheckStatus = (typeof DOCTOR_CHECK_STATUS_VALUES)[number];

/** Result of a single doctor check. */
export interface DoctorCheckResult {
  /** Check identifier. */
  id: string;
  /** Check status. */
  status: DoctorCheckStatus;
  /** Human-readable message. */
  message: string;
}

/** Aggregated doctor check results. */
export interface DoctorResult {
  /** Individual checks. */
  checks: DoctorCheckResult[];
}

/** Overall system status. */
export interface SystemStatus {
  /** Installed system version. */
  version: string;
  /** Whether the system is healthy. */
  healthy: boolean;
  /** Supporting checks. */
  checks: DoctorCheckResult[];
}

/** User-editable configuration. */
export interface OmpConfig {
  /** Preferred output locale. */
  locale: Locale;
  /** Preferred output mode. */
  outputMode: CliOutputMode;
}
