// This file is generated by tools/gen-contracts.mjs.
// Do not edit by hand.

import type { JsonValue, KernelWarning, Locale } from "./core.js";

/** Known deterministic skill identifier. */
export const SKILL_ID_VALUES = ["summarizeStatus", "extractRisks", "deriveNextTasks", "createHandoff", "reviewChanges"] as const;
export type SkillId = (typeof SKILL_ID_VALUES)[number];

/** Deterministic execution context injected into skills. */
export interface SkillExecutionContext {
  /** Requested output locale. */
  locale: Locale;
  /** Injected clock value. */
  now: string;
}

/** Envelope for a skill execution input. */
export interface SkillInputEnvelope {
  /** Target skill. */
  skillId: SkillId;
  /** Execution context. */
  context: SkillExecutionContext;
  /** Skill-specific input. */
  input: JsonValue;
}

/** Envelope for a skill execution output. */
export interface SkillOutputEnvelope {
  /** Executed skill. */
  skillId: SkillId;
  /** Whether execution succeeded. */
  ok: boolean;
  /** Skill-specific output. */
  output: JsonValue;
  /** Warnings. */
  warnings?: KernelWarning[];
}
