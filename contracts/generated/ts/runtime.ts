// This file is generated by tools/gen-contracts.mjs.
// Do not edit by hand.

import type { JsonValue, KernelError, KernelWarning, Locale } from "./core.js";

/** Kind of runtime request. */
export const RUNTIME_REQUEST_KIND_VALUES = ["status", "doctor", "plan", "executeSkill"] as const;
export type RuntimeRequestKind = (typeof RUNTIME_REQUEST_KIND_VALUES)[number];

/** Request handled by the runtime. */
export interface RuntimeRequest {
  /** Request identifier. */
  id: string;
  /** Request kind. */
  kind: RuntimeRequestKind;
  /** Requested output locale. */
  locale: Locale;
  /** Kind-specific payload. */
  payload: JsonValue;
}

/** Response produced by the runtime. */
export interface RuntimeResponse {
  /** Request identifier. */
  id: string;
  /** Whether the request succeeded. */
  ok: boolean;
  /** Response payload. */
  data: JsonValue;
  /** Warnings. */
  warnings?: KernelWarning[];
  /** Error when the response failed. */
  error?: KernelError;
  /** Deterministic execution trace. */
  trace?: ExecutionTraceEntry[];
}

/** Single entry in an execution trace. */
export interface ExecutionTraceEntry {
  /** Step name. */
  step: string;
  /** Step status. */
  status: string;
  /** Optional detail message. */
  message?: string;
}

/** Result of executing a single task node. */
export interface NodeResult {
  /** Task node identifier. */
  nodeId: string;
  /** Whether the node succeeded. */
  ok: boolean;
  /** Node output. */
  data: JsonValue;
}
