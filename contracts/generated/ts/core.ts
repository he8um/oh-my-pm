// This file is generated by tools/gen-contracts.mjs.
// Do not edit by hand.

/** JSON-compatible value. */
export type JsonValue = null | boolean | number | string | JsonValue[] | { [key: string]: JsonValue };

/** Stable machine-readable error code. */
export type OmpErrorCode = string;

/** Non-blocking warning returned by the Kernel. */
export interface KernelWarning {
  /** Warning code. */
  code: string;
  /** Human-readable message. */
  message: string;
}

/** Stable machine-readable error returned by the Kernel. */
export interface KernelError {
  /** Registered error code. */
  code: OmpErrorCode;
  /** Human-readable message. */
  message: string;
  /** Location of the failure. */
  path?: string;
  /** Whether the failure blocks the operation. */
  blocking: boolean;
  /** Structured, secret-free context. */
  details?: JsonValue;
}

/** Kernel operation result. */
export type KernelResult = KernelResultOk | KernelResultErr;

export interface KernelResultOk {
  status: "ok";
  /** Operation data. */
  data: JsonValue;
  /** Warnings. */
  warnings: KernelWarning[];
}

export interface KernelResultErr {
  status: "error";
  /** Error. */
  error: KernelError;
  /** Warnings. */
  warnings: KernelWarning[];
}

/** Supported output locale. */
export const LOCALE_VALUES = ["en", "fa"] as const;
export type Locale = (typeof LOCALE_VALUES)[number];

/** Finding severity. */
export const SEVERITY_VALUES = ["info", "warning", "error"] as const;
export type Severity = (typeof SEVERITY_VALUES)[number];

/** Report output format. */
export const REPORT_FORMAT_VALUES = ["brief", "json", "markdown"] as const;
export type ReportFormat = (typeof REPORT_FORMAT_VALUES)[number];

/** Envelope for machine-readable reports. */
export interface ReportEnvelope {
  /** Envelope schema version. */
  schemaVersion: string;
  /** Report kind identifier. */
  kind: string;
  /** Report creation time supplied by the caller. */
  generatedAt: string;
  /** Report format. */
  format: ReportFormat;
  /** Report payload. */
  data: JsonValue;
  /** Warnings. */
  warnings?: KernelWarning[];
}
