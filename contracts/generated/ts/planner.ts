// This file is generated by tools/gen-contracts.mjs.
// Do not edit by hand.

import type { JsonValue, Locale } from "./core.js";

/** Recognized planning intent. */
export const INTENT_CATEGORY_VALUES = ["status", "riskReview", "nextTask", "planning", "handoff"] as const;
export type IntentCategory = (typeof INTENT_CATEGORY_VALUES)[number];

/** Kind of task node. */
export const TASK_NODE_KIND_VALUES = ["providerRead", "skillExecution"] as const;
export type TaskNodeKind = (typeof TASK_NODE_KIND_VALUES)[number];

/** Single node in a task graph. */
export interface TaskNode {
  /** Node identifier. */
  id: string;
  /** Node kind. */
  kind: TaskNodeKind;
  /** Human-readable title. */
  title: string;
  /** Identifiers of prerequisite nodes. */
  dependsOn: string[];
  /** Kind-specific payload. */
  payload: JsonValue;
}

/** Directed acyclic graph of task nodes. */
export interface TaskGraph {
  /** Graph nodes. */
  nodes: TaskNode[];
}

/** Request for context the planner still needs. */
export interface MissingContextRequest {
  /** Why the context is needed. */
  reason: string;
  /** Requested context keys. */
  requestedContext: string[];
}

/** Input for a planning run. */
export interface PlannerInput {
  /** User request text. */
  request: string;
  /** Requested output locale. */
  locale: Locale;
  /** Available project context. */
  context: JsonValue;
}

/** Result of a planning run. */
export type PlannerResult = PlannerResultOk | PlannerResultMissingContext;

export interface PlannerResultOk {
  status: "ok";
  /** Planned task graph. */
  graph: TaskGraph;
}

export interface PlannerResultMissingContext {
  status: "missingContext";
  /** Missing context request. */
  missingContext: MissingContextRequest;
}
