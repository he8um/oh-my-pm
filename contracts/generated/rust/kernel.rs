// This file is generated by tools/gen-contracts.mjs.
// Do not edit by hand.

use serde::{Deserialize, Serialize};
use std::collections::BTreeMap;
use super::core::{OmpErrorCode};

/// Release lifecycle state.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub enum ReleaseState {
    Idea,
    Source,
    Validation,
    Ready,
    Released,
    Published,
    Frozen,
}

/// Allowed release state transitions.
pub fn allowed_transitions() -> BTreeMap<String, Vec<ReleaseState>> {
    {
        let mut map = BTreeMap::new();
        map.insert("idea".to_string(), vec![ReleaseState::Source]);
        map.insert("published".to_string(), vec![ReleaseState::Frozen]);
        map.insert("ready".to_string(), vec![ReleaseState::Released]);
        map.insert("released".to_string(), vec![ReleaseState::Published]);
        map.insert("source".to_string(), vec![ReleaseState::Validation]);
        map.insert("validation".to_string(), vec![ReleaseState::Ready, ReleaseState::Source]);
        map
    }
}

/// Requested release state transition.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct StateTransitionInput {
    /// Current state.
    pub from: ReleaseState,
    /// Requested target state.
    pub to: ReleaseState,
}

/// Decision for a requested release state transition.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct StateTransitionDecision {
    /// Current state.
    pub from: ReleaseState,
    /// Requested target state.
    pub to: ReleaseState,
    /// Whether the transition is allowed.
    pub allowed: bool,
    /// Decision reason.
    pub reason: String,
}

/// Known component identifier.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub enum ComponentId {
    Contracts,
    Kernel,
    Runtime,
    Planner,
    Providers,
    Skills,
    Cli,
    Installer,
    Validation,
}

/// Version entry for a single component.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ComponentVersion {
    /// Component identifier.
    pub id: ComponentId,
    /// Semantic version.
    pub version: String,
    /// Release state.
    pub state: ReleaseState,
}

/// Single source of installed component versions.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct VersionRegistryManifest {
    /// Manifest schema version.
    pub schema_version: String,
    /// Overall system version.
    pub system_version: String,
    /// Component entries.
    pub components: Vec<ComponentVersion>,
}

/// Known feature flag key.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub enum FeatureFlagKey {
    ReleaseEnabled,
    AssistedPlanning,
    DashboardPreview,
}

/// Resolved feature flag values.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct FeatureFlags {
    /// Whether releases are enabled.
    pub release_enabled: bool,
    /// Whether assisted planning is enabled.
    pub assisted_planning: bool,
    /// Whether the dashboard preview is enabled.
    pub dashboard_preview: bool,
}

/// Payload kind accepted by the validation engine.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub enum ValidationTarget {
    SystemRequest,
    TaskGraph,
    ProviderResponse,
    SkillInput,
    SkillOutput,
    VersionRegistry,
    UpdatePlan,
    InstallManifest,
}

/// Single validation finding.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ValidationFinding {
    /// Registered error code.
    pub code: OmpErrorCode,
    /// Human-readable message.
    pub message: String,
    /// JSON pointer into the payload.
    pub path: String,
    /// Whether the finding blocks the operation.
    pub blocking: bool,
}

/// Aggregated validation findings for one payload.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct ValidationReport {
    /// Validated payload kind.
    pub target: ValidationTarget,
    /// True when no blocking errors exist.
    pub passed: bool,
    /// Blocking findings.
    pub errors: Vec<ValidationFinding>,
    /// Non-blocking findings.
    pub warnings: Vec<ValidationFinding>,
}

/// Kind of update plan step.
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub enum UpdatePlanStepKind {
    Create,
    Replace,
    Delete,
    Verify,
}

/// Single step in an update plan.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UpdatePlanStep {
    /// Step kind.
    pub kind: UpdatePlanStepKind,
    /// Install-root-relative path.
    pub path: String,
    /// Expected content checksum.
    #[serde(skip_serializing_if = "Option::is_none")]
    pub checksum: Option<String>,
}

/// Snapshot allowing an update to be reverted.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct RollbackPoint {
    /// Rollback point identifier.
    pub id: String,
    /// Creation time supplied by the caller.
    pub created_at: String,
    /// Captured paths.
    pub paths: Vec<String>,
}

/// Guarded plan describing an update.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(rename_all = "camelCase")]
pub struct UpdatePlan {
    /// Plan identifier.
    pub id: String,
    /// Currently installed version.
    pub from_version: String,
    /// Target version.
    pub to_version: String,
    /// Ordered steps.
    pub steps: Vec<UpdatePlanStep>,
    /// Rollback snapshot.
    pub rollback: RollbackPoint,
}

/// Guard verdict for an update plan.
#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)]
#[serde(tag = "status")]
pub enum UpdateGuardDecision {
    #[serde(rename = "allowed", rename_all = "camelCase")]
    Allowed {
        /// Plan identifier.
        plan_id: String,
        /// Hash binding the decision to the plan.
        plan_hash: String,
        /// Decision reasons.
        reasons: Vec<String>,
    },
    #[serde(rename = "blocked", rename_all = "camelCase")]
    Blocked {
        /// Plan identifier.
        plan_id: String,
        /// Hash binding the decision to the plan.
        plan_hash: String,
        /// Decision reasons.
        reasons: Vec<String>,
    },
}
