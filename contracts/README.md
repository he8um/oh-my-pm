# @oh-my-pm/contracts

Shared data contracts for OH MY PM.

This package owns the public data shapes shared across the workspace. Contract definitions are authored as JSON schema declarations under `schema/` and generated into TypeScript and Rust outputs under `generated/`.

Generated files are committed and must not be edited by hand.
