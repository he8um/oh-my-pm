[package]
name = "oh-my-pm-kernel"
version = "0.2.0-alpha.0"
edition = "2021"
license = "MIT"
description = "Pure deterministic Kernel for OH MY PM"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
wasm-bindgen = "0.2"
