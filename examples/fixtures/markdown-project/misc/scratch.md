# Scratch Notes

This scratch document lives outside the example include rules (`README.md` and
`docs/**/*.md`), so it is never selected. Its unique sentinel content would
pollute every workflow if it were loaded, so tests assert the sentinel never
appears in derived output.

## Blocked

- SCRATCH-SENTINEL a scratch blocker that must never surface in a risk report.

## Next actions

- [ ] SCRATCH-SENTINEL scratch task that must never appear in a next-task list.

## Decisions

- Decision: SCRATCH-SENTINEL scratch decision that must never appear in a
  handoff.
