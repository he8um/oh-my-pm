# Obsolete Plan (archived)

This archived document is deliberately excluded by the example
`oh-my-pm.config.json`. If it were ever loaded, its unique sentinel content
below would visibly pollute every workflow, so tests assert the sentinel never
appears in derived output.

## Blocked

- ARCHIVED-SENTINEL the retired vendor contract is blocked and must never
  surface in a project risk report.

## Next actions

- [ ] ARCHIVED-SENTINEL archived task that must never appear in a next-task list.

## Decisions

- Decision: ARCHIVED-SENTINEL retired decision that must never appear in a
  handoff.
