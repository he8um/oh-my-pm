# v0.6.2 Project Memory integrity audit

Audit of the Project Memory integrity guarantees as they exist at
`ed9299e33e437f80043255c84f4e09675da82a4d` (post-v0.6.1 `main`), performed before
any v0.6.2 implementation work.

**Why this document exists first.** The original v0.6.2 scope was written as if
atomic writes, integrity metadata, migrations, locking, path confinement, and
privacy enforcement all needed building. They do not — most of them shipped
earlier and are genuinely implemented, not stubbed. Building a second atomic-write
layer beside a correct one would make the system worse, so the release was
re-scoped to **verification and gap closure** and this audit is the authority for
what that means.

Baseline at audit time: **116 tests across 13 suites** in `project-memory/test`,
all passing.

## Status vocabulary

| Status | Meaning |
| --- | --- |
| **Proven** | Implemented, and a test would fail if the guarantee broke. |
| **Under-proven** | Implemented and believed correct, but no test pins the guarantee. |
| **Partial** | Some of the guarantee exists; a named part is absent. |
| **Missing** | No implementation. |
| **Out of scope** | Deliberately excluded, with a reason. |

## Matrix

| # | Original scope item | Status | Evidence / gap |
| --- | --- | --- | --- |
| 1 | Authoritative data ownership | **Under-proven** | `assertProjectDataSeparation` enforces the project/data-root split both ways; ownership is not stated in one authoritative document. |
| 2 | Stored-data inventory | **Under-proven** | Layout is fully expressed in `path-safety.ts` (`projects/`, `snapshots/`, `evidence/`, `staging/`, `backups/`, `locks/`); no single document records owner, format, and recovery per artifact. |
| 3 | Atomic writes | **Partial** | `writeFileAtomic` does temp → `fsync` → `rename`, mode `0600` on POSIX, best-effort `syncDir`. Seven commit-sequence fail points exist. **No fault injection reaches inside `writeFileAtomic`** — four of the nine required stages (before temp write, during temp write, after flush, before parent sync) are unreachable. |
| 4 | Checksums / integrity metadata | **Proven** | `integrity.ts` domain-separated digests; `manifest.ts` verifies on parse; `corruption.test.ts` proves a tampered payload is rejected. |
| 5 | Migration framework | **Proven** | Ordered registry, `planMigration`, backups, migration history, post-failure preservation, no auto-migration on read, deterministic chronology with `capturedAt`-then-id tie-break, backup-readable recovery. |
| 6 | Compatibility policy | **Proven** | Unsupported newer store formats are refused (`corruption.test.ts`); reads never rewrite. |
| 7 | Concurrency / locking | **Partial** | Exclusive-create lock; stale requires **age AND dead owner**; injected clock and liveness probe. Logic covered by six tests — but **entirely in-process against `MemoryFileSystem`**. No test spawns a real OS process, so `open(path, "wx")` on a real filesystem is unproven. |
| 8 | Path confinement | **Partial** | `isSameOrInside` uses `relative()` + `..` check, correctly avoiding the prefix-string bug. Reads refuse to follow symlinks at the leaf; record directories reject symlinks. **Gaps:** containment is purely lexical (`resolve`/`normalize` never consult the filesystem); no junction, reparse-point, nested-symlink, or case-insensitive coverage; the write path performs no `lstat` while the read path does. |
| 9 | Corruption handling | **Partial** | Detection is strong: malformed JSON, missing referenced record, integrity mismatch, wrong-project record, unsupported format, symlink in a record directory, abandoned staging — and `inspect()` provably does not auto-repair. **No quarantine or isolation exists**; `quarantine` and `repair` appear nowhere in the source. |
| 10 | Repair tooling | **Missing** | No preview, no apply, no internal tooling. |
| 11 | Cache policy | **Out of scope** | No cache exists. Adding one would violate "do not add a cache without real need." |
| 12 | Retention | **Under-proven** | `limits.ts` states history is never auto-pruned, which is the correct non-destructive default. No test pins it, so a future change could silently introduce deletion. |
| 13 | Privacy | **Proven** | `privacy.ts` forbidden-key enforcement with `privacy.test.ts`. Store is local-only; no network, telemetry, or account code anywhere in the package. |
| 14 | Application boundary integration | **Proven** | v0.6.1 boundary intact; `validate-application-boundary` enforces it. |
| 15 | Crash safety | **Partial** | Same root cause as item 3, plus a distinct defect: the crash test at `store-commit.test.ts:125` constructs a **fresh** `MemoryFileSystem` for its follow-up commit, so it never writes against the damaged store. Its own comment claims it proves the lock was released and "a follow-up commit works" — it proves neither. **Post-crash recovery is untested.** |
| 16 | Data consistency | **Proven** | `assertManifestChronology`; manifest/record agreement verified on read; duplicate and orphan detection in `verify()`. |
| 17 | Performance bounds | **Missing** | No performance or bounded-behaviour test of any size class. |
| 18 | Documentation | **Under-proven** | Behaviour is documented in module headers; no release-level statement of authority and recovery guarantees. |

## Confirmed material gaps

Five gaps are real, evidence-backed, and each changes the confidence of a shipped
guarantee. They are the whole of the v0.6.2 implementation scope.

### G1 — Fault injection cannot reach inside the atomic write (item 3)

The seven fail points are all at the commit-sequence level. `writeFileAtomic` is
a single opaque step, so four required stages cannot be tested at all:
before temp write, during temp write, after flush, before parent sync.

The write may well be correct; nothing proves it. **Fix:** extend the fail-point
enumeration into the filesystem port so injection can occur inside the write, and
add the missing stage tests. Do not replace `writeFileAtomic`.

### G2 — Post-crash recovery is untested, and one test misreports itself (item 15)

`store-commit.test.ts:125` creates `new MemoryFileSystem()` for the follow-up
commit — a pristine filesystem with no lock file, no staging residue, no partial
state. The genuine property (a later write against the *same damaged* store
succeeds) is unverified.

This is the most misleading finding in the audit: the test reads as if crash
recovery is covered. **Fix:** reuse the crashed filesystem, and assert the lock is
gone, residue is detectable, and the next commit succeeds.

### G3 — Locking is never exercised across real processes (item 7)

The stale rule (age AND dead owner) is correct and must not be weakened. But the
lock's real contract is `open(path, "wx")` on a real filesystem between real
processes, and every existing test is in-process against an in-memory port.

**Fix:** add separate-process contention tests using the real Node adapter —
two processes cannot both write, a live slow writer is never evicted, a dead
stale owner is reclaimed, acquisition is bounded, crash residue recovers.

### G4 — Path confinement is lexical, and writes are less protected than reads (item 8)

Containment never consults the filesystem, so a symlink or junction inside the
data root pointing outside satisfies `isSameOrInside`. Reads mitigate this by
refusing symlinks at the leaf; the write path performs no such check.

Severity is bounded — managed paths are SHA-256-derived under a confined root, so
exploiting this needs prior write access to the store — but the read/write
asymmetry is a genuine inconsistency. **Fix:** add junction, reparse-point,
nested-symlink, and case-insensitive coverage; close the write-path asymmetry if
the tests confirm it is reachable.

### G5 — Corruption can be detected but never isolated or repaired (items 9, 10)

Detection is strong and deliberately non-destructive. But a user with a corrupt
record has no supported path forward: no quarantine, no preview, no repair.

**Fix:** preview-first quarantine that isolates a corrupt record, preserves it as
evidence, and keeps the remaining valid history readable. Never delete or
overwrite an authoritative record. Extend the existing memory command surface only
if a public command is justified; otherwise keep it internal and say so.

## Non-gaps — do not "fix" these

- **Atomic write mechanism.** Temp → fsync → rename with `0600` and best-effort
  directory fsync is correct. The gap is provability, not design.
- **Stale-lock rule.** Requiring age **and** a dead owner avoids evicting a live
  slow writer. Do not relax it to simplify a test.
- **No auto-repair on read.** Deliberate and proven. Reads must never mutate.
- **No cache.** Nothing needs one.
- **No auto-pruning.** The non-destructive default is correct; it needs a test,
  not a change.
- **Prefix containment.** `relative()` + `..` already avoids the string-prefix
  bug.
- **Domain-separated digests.** Distinct domain constants per record kind already
  prevent cross-context digest reuse.

## Release justification

The re-scoped release threshold requires at least one material integrity or
verification gap whose closure changes the confidence of the shipped system.

**Met.** G2 is a test that misreports what it proves; G3 leaves the cross-process
contract of a single-writer lock unverified; G1 makes four of nine required crash
stages untestable; G5 leaves users with no recovery path from a detected
corruption. Each changes what can honestly be claimed about the system.

## Deferred, with reasons

- **Cache policy (item 11)** — no cache exists and none is needed.
- **Full performance architecture** — v0.6.5. This release records bounded
  behaviour baselines only, and invents no SLOs.
- **New schema version** — explicitly not invented to exercise the migration
  framework; `migration1to2` and historical fixtures already exercise it.
- **Stale `README` / `SECURITY.md` validator gap** — `validate-doc-truth` does not
  check the README badge block, which is how a `v0.5.3` latest-stable claim
  survived three releases. Corrected in the v0.6.1 truth PR, but the *validator*
  gap remains and is recorded as separate future work, not mixed into this
  release.

---

# Closure

Added after implementation. The matrix above is the audit **as performed at
`ed9299e33e437f80043255c84f4e09675da82a4d`** and is left unedited: it records what
was believed before the work, which is what makes the difference between an audit
and a summary. This section records what is now proven.

## Closure status vocabulary

| Status | Meaning |
| --- | --- |
| **proven** | A test fails if the guarantee breaks. |
| **implemented_and_proven** | Built in this release, with tests that fail if it breaks. |
| **proven_with_platform_limit** | Proven, with a named platform whose behaviour local runs cannot establish. |
| **intentionally_out_of_scope** | Deliberately excluded, with a reason. |
| **deferred_with_explicit_blocker** | Not done, with the blocker named. |

## Final matrix

| # | Item | Closure status | Evidence |
| --- | --- | --- | --- |
| 1 | Authoritative data ownership | **proven** | The ownership model below; `assertProjectDataSeparation` enforced on every mutating entry point including `applyRepair`; a CLI test digests the project tree before and after an apply. |
| 2 | Stored-data inventory | **proven** | The stored-data inventory below, one row per governed artifact. |
| 3 | Atomic writes | **proven** | `atomic-write.ts` (`d6fe23e`) extracts the sequence so all nine stages are reachable; `atomic-write.test.ts` covers each. |
| 4 | Checksums / integrity metadata | **proven** | Unchanged from the audit; `corruption.test.ts`. |
| 5 | Migration framework | **proven** | Unchanged; `migration.test.ts`, `migration-chronology.test.ts`. |
| 6 | Compatibility policy | **proven** | A future store format is refused and never downgraded — `repair.test.ts` proves the scan reports it `unrepairable` and the plan contains no action touching it. Mutation M7 kills the alternative. |
| 7 | Concurrency / locking | **proven_with_platform_limit** | `process-lock.test.ts` (`be5a34e`) spawns real subprocesses against the real Node adapter. `applyRepair` takes the same lock and releases it in `finally`; mutations M3 and the crash-stage suite prove both. Windows behaviour rests on CI, not local runs. |
| 8 | Path confinement | **proven_with_platform_limit** | `physical-confinement.ts` (`45f8289`) resolves one link level at a time; quarantine writes inherit it because they route through the same guarded adapter methods. Junction behaviour rests on Windows CI. Residual TOCTOU is stated, not claimed closed. |
| 9 | Corruption handling | **implemented_and_proven** | Twelve classification codes with authority classes; `repair.test.ts`. |
| 10 | Repair tooling | **implemented_and_proven** | Preview/apply with quarantine, receipts, and a CLI surface; store suite plus `cli/test/memory-repair.test.ts` against a real store. |
| 11 | Cache policy | **intentionally_out_of_scope** | No cache exists. Nothing needs one; adding one would create eviction semantics with no beneficiary. |
| 12 | Retention | **proven** | No automatic pruning, no automatic quarantine deletion. `repair.test.ts` proves an apply removes only what the plan named; quarantine is never a repair target. |
| 13 | Privacy | **proven** | `privacy.test.ts`, plus repair-specific proofs: no absolute path, no raw record content, and no secret-like value reaches a finding, metadata file, or CLI output. Mutation M9 kills the alternative. |
| 14 | Application boundary integration | **proven** | `repair` reaches the CLI through `runMemoryProcess`; `validate-application-boundary` passes; no second presentation consumer added. |
| 15 | Crash safety | **proven** | `74c40fd` fixed the test that reused a pristine filesystem. Eleven repair failure stages each prove bytes stay recoverable, no invented state appears, the lock is released, and a retry converges. |
| 16 | Data consistency | **proven** | Derived state is rebuilt only from records that verify after isolation; chronology is filtered and renumbered contiguously, never re-derived from a clock. Mutation M6 kills the alternative. |
| 17 | Performance bounds | **proven** | Bounded structural baselines (Phase E) over deterministic fixtures. Not SLOs — see the deferral note. |
| 18 | Documentation | **proven** | This closure, the v0.6.2 release note, `cli/README.md`, and the `docs/` set. |

## Authority and ownership model

What may be done to a byte depends entirely on who owns it.

```text
project source files:
  authority: user-owned source
  mutation by repair: forbidden

Project Memory records:
  authority: authoritative memory state
  mutation by repair: isolation only; never rewritten from a guess, never deleted

manifest and indexes:
  authority: derived
  rebuildable: yes, from verified records only

quarantine:
  authority: recovery evidence
  normal history: no
  auto deletion: no

temporary files:
  authority: none
  removable: only with proven ownership

locks:
  authority: coordination state
  reclaimable: age threshold AND dead owner

cache:
  exists: no
```

Two of these deserve emphasis because getting them wrong loses data rather than
merely misbehaving:

- **Derived state is rebuilt only from verified records.** A rebuild that trusted
  the existing inventory would re-reference records the same apply just isolated; a
  rebuild that trusted whatever is on disk would launder corruption into
  apparently-valid authoritative state.
- **Quarantine is not repair.** Isolating a corrupt record restores the
  readability of the rest of the store. It does not recover that record's meaning.
  Every output path keeps `isolated` and `reconstructed` as separate counters for
  exactly this reason.

## Stored-data inventory

| Artifact | Layout | Owner | Format | Authority | Read | Write | Atomicity | Integrity | Confinement | Locking | Recovery | Retention | Privacy |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Manifest | `projects/<key>/manifest.json` | project-memory | JSON | derived projection over authoritative records | `readManifest` | commit, migrate, repair rebuild | temp→fsync→rename | domain-separated SHA-256 over canonical body | lexical + physical | writer lock | rebuilt from verified records; quarantined if itself damaged | never auto-pruned | no path, no token, no raw content |
| Snapshot record | `projects/<key>/snapshots/<key>.json` | project-memory | JSON envelope | authoritative | `readSnapshot` | commit only | temp→fsync→rename | envelope digest | lexical + physical | writer lock | quarantine (isolation) | never auto-pruned | minimized payload; forbidden keys rejected |
| Evidence record | `projects/<key>/evidence/<key>.json` | project-memory | JSON envelope | authoritative | `readEvidence` | commit only | temp→fsync→rename | envelope digest | lexical + physical | writer lock | quarantine (isolation) | never auto-pruned | minimized payload; forbidden keys rejected |
| Staging | `projects/<key>/staging/` | project-memory | JSON | none (residue) | `inspect` reports | commit | n/a | n/a | lexical + physical | writer lock | removed when ownership proven | not auto-pruned outside its own commit | n/a |
| Backups | `projects/<key>/backups/<op>/` | project-memory | JSON | pre-migration evidence | manual | migration | file copy | inherited | lexical + physical | writer lock | read manually | never auto-deleted | as records |
| Quarantine payload | `projects/<key>/quarantine/<op>/<entry>/payload.bin` | project-memory | exact original bytes | recovery evidence | manual | repair apply | temp→fsync→rename, confirmed by re-read | digest recorded in metadata | lexical + physical | writer lock | is itself the recovery artifact | never auto-deleted | bytes are the user's own; never echoed to output |
| Quarantine metadata | `.../metadata.json` | project-memory | JSON | recovery evidence | manual | repair apply | temp→fsync→rename | digest of payload | lexical + physical | writer lock | n/a | never auto-deleted | classification + digests only; no parsed content |
| Repair receipt | `.../receipt.json` | project-memory | JSON | audit record | manual | repair apply | temp→fsync→rename | n/a | lexical + physical | writer lock | n/a | never auto-deleted | sanitized outcomes only |
| Lock | `locks/<key>.lock` | project-memory | JSON | coordination | `readLock` | exclusive create | `open(path,"wx")` | n/a | lexical + physical | is the lock | reclaimed on age AND dead owner | removed on release | pid + operation id only; no project path |

There is no cache row because there is no cache.

## Compatibility

Proven for this release:

- every valid v0.6.1 store remains readable — the store format is unchanged at
  v2 and no migration was introduced;
- migration fixtures still pass (`migration.test.ts`, `migration-chronology.test.ts`);
- a future store format is rejected without rewrite;
- repair preview is non-mutating, proven by whole-tree digest in both the store
  suite and a real-filesystem CLI test;
- apply does not modify project source files;
- existing CLI outputs are unchanged outside the additive `repair` command;
- MCP tool count, names, order, schemas, and annotations are unchanged
  (`pnpm mcp:smoke`, `pnpm validate`).

## Corrected evidence incidents

Recorded permanently rather than tidied away, because the lesson is the
deliverable.

1. **Invalid mutation evidence.** A Vitest filter containing `|` was assumed to be
   a regex. Vitest treats the filter as a literal substring, so it matched zero
   files and the command exited 1. The nonzero exit was read as the mutation being
   caught; nothing had run.
2. **Vacuous assertion.** A G3 assertion filtered on `issue.severity`, a property
   that does not exist on the inspection issue type. The filter matched nothing, so
   the assertion passed no matter how damaged the store was.
3. **Near-miss during this closure.** The doc-truth test that claimed to *derive*
   the memory subcommand count asserted a hardcoded `7`. It was corrected to derive
   the count on both sides — and the first attempt to prove the correction by
   mutation silently did not apply, which is the same pattern as incident 1. The
   harness now fails loudly when a mutation does not change the file.

The rule this enforces:

```text
a failing command is not mutation evidence
unless the unmutated baseline passes,
the intended tests are discovered,
the mutation changes the intended behaviour,
and targeted assertions fail
```

A corollary learned here: a *flaky* baseline is as disqualifying as a failing one.
`b63fe84` fixed an unhandled-rejection race that failed 2 of 8 runs; mutation
evidence gathered against that baseline would have been worthless.

## Residual limitations

Stated plainly rather than left for a reader to discover.

- **TOCTOU is reduced, not eliminated.** Physical confinement validates
  immediately before each mutating syscall, but a privileged concurrent actor can
  replace an ancestor in that window. Closing it fully needs descriptor-relative
  operations (`openat`/`O_NOFOLLOW` per segment) that Node does not expose.
- **Power-loss durability is not proven.** Injecting a stage failure proves the
  algorithm's recovery contract — which states are reachable and whether a retry
  reconciles. Real power-loss durability remains bounded by the platform's own
  fsync semantics and cannot be established by a simulated filesystem.
- **Directory fsync is best-effort.** Some platforms refuse it; the rename remains
  the commit point.
- **Platform evidence comes from CI.** Local runs skip POSIX-only and
  Windows-only behaviour, so the `project-memory-integrity` matrix is the authority
  for Windows and Linux.
- **A store with no manifest is not reconstructed.** Records may survive, but
  nothing authoritative says which are current, and capture chronology is never
  invented.

## Deferred

- **Cache policy** — no cache exists and none is needed.
- **Full performance architecture** — v0.6.5. This release records bounded
  structural baselines and invents no SLOs.
- **New schema version** — not invented merely to exercise the migration
  framework.
- **`validate-doc-truth` README badge gap** — the validator still does not check
  the README badge block. Recorded as separate future work, not folded into this
  release.
