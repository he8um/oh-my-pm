# OH MY PM v0.2.0

Status: Unreleased (release candidate prepared)

Current candidate version: `0.2.0-rc.1` (prepared, not published)

This document tracks the `v0.2` line. The `v0.2` scope is now **frozen** and the
first release candidate `0.2.0-rc.1` has been prepared on `main`. Nothing is
published: there is no `v0.2.0-rc.1` tag, GitHub Release, or download, and the
latest stable release remains `v0.1.0`. Publication is a separate, manually gated
step (see [publishing-v0.2.0-rc.1.md](publishing-v0.2.0-rc.1.md)).

## Scope (frozen for v0.2)

The `v0.2` feature scope is complete and frozen; nothing below is outstanding
work for `v0.2`. The following capabilities are intentionally **deferred beyond
v0.2** and are not required to complete it:

- timeline events (a separate, later bounded phase)
- review-thread graph / resolution, reactions, commits/files/diffs
- provider aliases/profiles (only if justified by demonstrated usage)
- controlled write-back (separate future approval)
- HTTP MCP transport, telemetry, dashboard, registry publishing

## Already in the development baseline

- candidate version aligned to `0.2.0-rc.1` across `version.json`, all workspace
  manifests, runtime version constants, and the Rust/WASM Kernel (the `v0.2`
  development line opened at `0.2.0-alpha.0`)
- self-describing version, bundle, and archive verification (the version is
  derived from `version.json`, `RELEASE.json`, or asset filenames rather than
  hard-coded)
- preview-first self-installation from an extracted verified release bundle:
  every portable bundle ships `bin/oh-my-pm-install.mjs`, which installs a
  versioned, source-independent copy under an explicit `--prefix`. Preview is
  the default, `--apply` is required to write, and `--force` replaces only the
  exact managed targets. Implemented in the current development line; this does
  **not** publish a `v0.2` release, tag, or download.
- a strictly read-only, explicitly opt-in GitHub provider for repository
  metadata, issues, and pull requests, exposed through both the CLI (`github
  brief|risks|next|handoff`) and MCP (`github_project_*`). `GET`-only against a
  fixed `api.github.com` origin, REST API version `2026-03-10`, with an optional
  `OH_MY_PM_GITHUB_TOKEN`. Implemented in the current development line; this does
  **not** publish a `v0.2` release, tag, or download.
- asynchronous provider, Runtime, CLI, and MCP execution boundaries to support
  real read-only network providers, with local workflows unchanged and offline.
- source-aware, line-level deterministic risk and next-task extraction for
  Markdown and GitHub context, with English and Persian headings/markers, exact
  GitHub label and status rules, overdue inference from the injected timestamp,
  one risk/task per GitHub item, 20-risk and 10-task limits, and optional public
  provenance metadata. Rule-based only — no LLM, embedding, or fuzzy scorer. See
  [the deterministic extraction guide](../deterministic-extraction.md).
- strict, read-only provider configuration (`providers.json`) with optional
  GitHub `enabled`/`defaultRepository`/`defaultLimit`/`defaultSource`/`defaultState`
  defaults, plus offline `providers status`/`providers doctor` commands and an
  explicitly confirmed, single-request GitHub access diagnostic. The MCP surface
  gains `provider_status` and `github_provider_diagnostics`, for exactly ten
  tools. No secret is ever stored in configuration; the token stays in
  `OH_MY_PM_GITHUB_TOKEN`. See
  [provider configuration](../providers/configuration.md) and
  [provider diagnostics](../providers/diagnostics.md).
- explicit GitHub source selection across CLI and the four GitHub MCP workflow
  tools: `overview`, `repository`, `issues`, `pull-requests`, `item` (single
  issue/PR with type auto-detection), and repository-scoped `search`, each with
  `open`/`closed`/`all` state selection and search `kind` filtering. The default
  stays `overview` + `open`; user search terms can never override the injected
  repository/state/kind scope. `GET`-only, single page, no timelines/diffs, no
  write-back. See
  [GitHub source selection](../providers/github-source-selection.md).
- bounded, opt-in GitHub item comments for the `item` source: ordinary issue/PR
  conversation comments only, disabled by default, one page of at most 50, via a
  single extra `GET /repos/{owner}/{repo}/issues/{number}/comments`. Review
  comments, reviews, timeline events, diffs, and writes are excluded; the MCP
  projection exposes comment identity/author/timestamps/url but never the body.
  See [GitHub item comments](../providers/github-item-comments.md).
- bounded, opt-in pull-request reviews and inline review comments for the `item`
  source, only when the item is a pull request: disabled by default, one page of
  at most 20 each, via a single extra `GET /repos/{owner}/{repo}/pulls/{number}/reviews`
  and `GET /repos/{owner}/{repo}/pulls/{number}/comments`. Deterministic
  review-state and explicit Markdown-derived risk, task, and handoff signals are
  produced; timeline events, thread resolution, reactions, diffs, files, and
  commits are excluded; the MCP projection exposes review identity/state/file
  provenance but never a body, diff hunk, or commit id. An issue selected with
  review options fails after exactly one item-identification request. See
  [GitHub pull-request reviews](../providers/github-pr-reviews.md).
- cross-platform release-install CI parity: the `windows-release-install` job now
  proves — as the POSIX job already did — that a release installation survives
  deleting the extracted source bundle and moving the complete installed prefix,
  re-verifying the installed CLI and MCP after each operation (F-TEST-1). CI-only;
  no product, installer, bundle, or shim behavior changed. This does **not** make
  `v0.2` released or automatically RC-ready.
- centralized GitHub list limit constants: the list minimum/default/maximum
  (`1`/`50`/`100`) now have one canonical executable source in the provider package,
  imported by provider config, effective settings, the source-selection resolver, and
  the CLI parser; the `DEFAULT_GITHUB_PROVIDER_LIMIT` and `GITHUB_CLI_DEFAULT_LIMIT`
  aliases are retained, and MCP schemas and release verifiers keep their independent
  boundary-defense literals (F-DUP-1). Behavior-preserving refactor — no numeric value,
  flag, error, or schema changed.
- controlled live GitHub smoke against the real REST API using the **installed**
  portable artifact (not source imports): the tokenless provider flow (status,
  doctor, repository, issues, search) passed; a live PR-item flow with bounded
  comments/reviews/review-comments passed; the installed MCP live workflows
  (all four GitHub tools, exact ten-tool order) passed with bounded, sanitized
  projections; and both negative paths (issue + review options → controlled
  failure; invalid applicability → pre-network failure) passed. No forbidden
  discussion data (bodies, diff hunks, commit ids) appeared in any projection.
  Authenticated smoke was NOT RUN (token unavailable), which does not block RC.
  See [v0.2 live GitHub smoke](v0.2-live-github-smoke.md). Read-only validation
  only — no product behavior, version, tag, or release changed.

## Guarantees carried forward from v0.1.0

- no project file writes
- no context upload
- no telemetry
- stdio-only MCP transport
- Node.js 20+ runtime; no npm package
- local Markdown project workflows remain fully offline (no network, no token)

## New in the v0.2 development line

- Network access is off by default and scoped: only the explicit GitHub
  command/tool reaches the network, and only over read-only HTTPS `GET` requests
  to `api.github.com`. `RELEASE.json` declares this conditional, opt-in surface.
- Portable bundles ship a complete, prebuilt Node-loadable Kernel binding on
  Ubuntu, macOS, and Windows. Bundle assembly explicitly stages the generated
  JS glue, WASM binary, and CommonJS manifest and verifies them byte-for-byte;
  installation never rebuilds the Kernel, and the generated binding is never
  committed. This closes a Windows-only packaging gap where `pnpm deploy` omitted
  the generated Node WASM glue file.
